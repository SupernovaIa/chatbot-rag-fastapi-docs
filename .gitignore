# Secretos
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc

# Node
node_modules/

# Entornos
.venv/

# Config aislado de Claude Code
.cc-config/

# Ruido del SO
.DS_Store
Thumbs.db

# Wrapper personal de arranque (config aislado)
/cc

# Andamiaje de revisión del dataset gold (no es deliverable)
corpus/**/evals/REVIEW.md

# Salidas transitorias del runner de evals (artefactos de CI, no deliverables).
# baseline_metrics.json SÍ se versiona (lo refresca el workflow nocturno).
/eval_report.md
/eval_metrics.json
/eval_full_metrics.json
# Dump del índice pgvector cacheado en CI (actions/cache), no es deliverable.
/chunks_dump.sql

# Artefactos de LogBook (estado interno y docs generadas, no deliverables)
.logbook/
logbook/
