// Baseline shell for Bloque A. Chat UI (login, register, chat) lands in later blocks.
export default function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>Chatbot RAG · FastAPI docs</h1>
      <p>Frontend baseline operativo. La UI de chat llega en bloques posteriores.</p>
    </main>
  );
}
